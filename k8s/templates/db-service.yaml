apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Values.db.app }}
  labels:
    app: {{ .Values.db.app }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.db.app }}
  ports:
    - port: {{ .Values.db.port }}
      targetPort: {{ .Values.db.port }}